import styled from "styled-components";

const NavigationBreadcrumb = styled.h1`
  font-size: 1rem;
  font-family: ${({ theme }) => theme.fontFamily};
  font-weight: normal;
`;

export { NavigationBreadcrumb };
